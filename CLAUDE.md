# CLAUDE.md

本仓指令以 [AGENTS.md](AGENTS.md) 为准：fork/上游同步政策、安全分级（read/write/exec）、测试与集成门禁、Skill 路由。

快速命令：

```bash
uv sync && uv run pytest     # 测试
uvx blender-mcp              # stdio 运行（需 Blender addon 已连接）
```
